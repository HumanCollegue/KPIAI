// ExtractionPage
//
// Displays the line items extracted from the financial statement by Claude.
// Highlights flagged items and allows the CAM to proceed to KPI calculation.

export default function ExtractionPage() {
  return <div>Extraction Page — placeholder</div>;
}
