// UploadPage
//
// Allows a CAM to upload an audited annual financial statement (PDF or XLSX).
// On success, redirects to /extraction/:documentId to review extracted line items.

export default function UploadPage() {
  return <div>Upload Page — placeholder</div>;
}
